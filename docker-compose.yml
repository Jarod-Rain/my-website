services:
  sparklepawz:
    build: .
    container_name: rainbow-sparkle-pawz
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./music:/app/music
      - ./public:/app/public
      - ./server.js:/app/server.js
    environment:
      - PORT=3000
